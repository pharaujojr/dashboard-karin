# Configuração para ambiente Docker
spring.profiles.active=docker

# Configurações do banco de dados para Docker
spring.datasource.url=jdbc:postgresql://192.168.0.162:8449/dbsolturi
spring.datasource.username=solturi
spring.datasource.password=NuM0ea42Vj5mGPuqyv2N
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate para Docker
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Servidor para Docker
server.port=8080

# Thymeleaf para produção
spring.thymeleaf.cache=true

# Actuator para Docker
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.health.postgresql.enabled=true

# Logs para Docker
logging.level.com.exemplo.dashboardvendas=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN